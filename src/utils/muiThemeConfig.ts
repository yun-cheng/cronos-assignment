import { createTheme } from '@mui/material/styles'

const theme = createTheme({
	typography: {
		fontFamily: ['Manrope Variable', 'Helvetica', 'Arial', 'sans-serif'].join(
			','
		)
	}
})

export default theme
